package com.huertas.rivera.wikibusqueda.dao

/**
 * Interface para el acceso a datos local (DAO).
 * Siguiendo principios Senior, se separa la abstracción de la persistencia.
 */
interface SearchDao {
    // Aquí se podrían definir métodos para Room si se requiere persistencia local
    // como: suspend fun saveRecentSearch(query: String)
}
